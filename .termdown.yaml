mouse: true

# Drag to select text, copied on release. Takes click-drag away from the
# terminal's own selection while termdown is running (hold Shift, or Option
# on macOS, to fall back to it).
mouse-select: true
